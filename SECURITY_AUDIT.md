# Skill Security Audit

审计日期：2026-05-21

审计范围：本仓库中已同步的 45 个 Codex skill。此次审计只做静态检查，没有运行第三方 skill 脚本，也没有授权任何 skill 访问外部账户。

## 结论

仓库中未发现高置信度的真实密钥、令牌或账号密码。检出的敏感词主要来自说明文档、示例配置、环境变量名称或占位符。

整体风险不是来自“仓库里已经泄露了密钥”，而是来自部分 skill 具备读取本地凭据、调用外部 CLI、访问浏览器 Cookie、联网请求 API、修改飞书资源或执行本地清理动作的能力。后续使用这些 skill 时应按最小权限原则逐项启用。

## 高风险项

### claude-to-im

风险原因：

- 会保存 Telegram、Feishu、Discord、QQ、微信等通道的机器人令牌、应用密钥或会话配置。
- 会调用外部 CLI、打开浏览器或启动后台进程。
- 默认环境隔离策略可能继承较多本机环境变量。
- 仓库中的脚本和测试文件包含本地清理逻辑。

建议：

- 只在明确需要跨 IM 转发 Codex/Claude 会话时启用。
- 配置文件不要提交到 GitHub。
- 优先使用更严格的环境变量隔离配置。
- 不要运行服务安装、卸载、清理类脚本，除非已经逐行审阅。

### last30days

风险原因：

- 会读取环境变量、全局配置、本地配置文件，并支持从系统凭据存储和浏览器 Cookie 中寻找社媒登录凭据。
- 涉及 Reddit、X/Twitter、Bluesky、GitHub、Google、YouTube、Hacker News 等多个外部来源。
- 会在本地写入搜索结果、缓存或记忆文件。
- 内部脚本包含本地清理逻辑。

建议：

- 每次使用前确认具体数据源和账号范围。
- 不要允许自动扫描浏览器 Cookie，除非你明确知道它会读取什么。
- 单独保存 API Key，不要把 `.env`、Cookie、导出的凭据文件同步到仓库。

### lark-* 飞书 skill 集合

风险原因：

- 已安装的 lark skill 覆盖文档、云盘、多维表格、电子表格、消息、日历、任务、知识库、审批等能力。
- 多个 skill 不只是读取，也具备创建、更新、移动、分享、授权、上传、导出、导入等修改能力。
- 如果账户权限较大，误操作会影响真实飞书空间中的文档、表格、群消息或任务。

建议：

- 保持当前只开通常用域的思路，优先使用 `docs`、`drive`、`sheets`、`base` 等必要权限。
- 执行写入、批量更新、移动、权限变更前，先让 Codex 列出目标对象和操作清单。
- 涉及删除或不可逆操作时，由你手动处理，不让 AI 自动执行。
- 定期到飞书开放平台或授权页面复查已授予权限。

### project-wrapup-summary

风险原因：

- skill 本体是总结模板，风险很低。
- 但附带安装脚本包含覆盖安装前的本地清理步骤，和当前项目的“AI 不执行删除操作”安全原则冲突。

建议：

- 使用这个 skill 时只读取 `SKILL.md`。
- 安装或更新时采用非破坏式复制；如果确实需要移除旧版本，由你手动删除后再继续。

### hatch-pet

风险原因：

- skill 主要用于生成和验证 Codex pet 资源。
- 文档中包含生成图片后的本地清理步骤，和当前项目的“AI 不执行删除操作”安全原则冲突。

建议：

- 使用时跳过自动清理步骤。
- 需要清理临时文件时，让 Codex列出路径，由你手动处理。

## 中风险项

### seo-geo

风险原因：

- 会读取 DataForSEO 账号环境变量。
- 会向外部 SEO/GEO API 发起联网请求。
- 可能产生 API 费用或暴露待分析网站、关键词、竞争对手信息。

建议：

- 使用前确认 API 费用和请求范围。
- 不要把账号变量写进仓库文件。

### skill-creator

风险原因：

- 用于生成或改造 skill，可能调用外部模型 CLI 进行评估或迭代。
- 如果输入中包含私密业务规则，可能被带入模型调用上下文。

建议：

- 创建公开 skill 前先脱敏。
- 生成后的 skill 需要人工复核触发条件和脚本引用。

## 低风险项

以下 skill 当前主要是说明、提示词、清单或最佳实践，未发现明显的本地敏感读取或高危自动化行为：

- `ai-seo`
- `backlink-analyzer`
- `chrome-devtools`
- `competitor-profiling`
- `find-skills`
- `github-actions-docs`
- `gpt-image-2`
- `humanizer-zh`
- `marketing-ideas`
- `programmatic-seo`
- `requesting-code-review`
- `seo-audit`
- `ui-ux-pro-max`
- `vercel-react-best-practices`

其中 `chrome-devtools` 虽然本体偏调试，但它能操作浏览器页面；用于登录态页面时，仍应确认目标网址和操作步骤。

## 使用准则

1. 第三方 skill 首次使用前，先读 `SKILL.md`。
2. 含脚本、安装器、后台服务、浏览器 Cookie、凭据读取、云文档写入能力的 skill，默认按高风险处理。
3. API Key、Bot Token、Cookie、飞书 App Secret、账号配置文件不要提交到 GitHub。
4. 执行飞书写入任务前，先让 Codex 输出待修改对象、修改字段和预期结果。
5. 本地文件清理、远程文档删除、权限回收等不可逆操作，必须由用户手动完成。

## 后续建议

- 为仓库增加一份 `AGENTS.md`，明确“AI 不自动执行删除操作、第三方脚本先审计后运行、凭据文件不得提交”。
- 每次新增第三方 skill 后，补充本文件的审计结论。
- 对 `claude-to-im`、`last30days`、`lark-*` 建议单独做更细的逐文件审计，再决定是否长期启用。
